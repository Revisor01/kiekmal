# Continue Here: Plan 01-03 Checkpoint

## Status
- Task 1 (Fastify + Vitest): ✓ committed (1d86e4d)
- Task 2 (Dockerfile + docker-compose.prod.yml): ✓ committed (1de2bb2)
- Task 3 (Deployment verification): IN PROGRESS

## What's Working
- API container runs on server.godsapp.de (prayback_api_1)
- Health endpoint responds internally: `{"status":"ok","timestamp":"..."}`
- DB container runs (prayback_db_1), migrations applied
- Docker build succeeds after multiple Dockerfile fixes (hoisted deps, ESM)

## What Needs Fixing
- **Traefik routing not working**: Container has traefik labels but `curl -H 'Host: kiek-mal.de' http://127.0.0.1:8888/health` returns 404
- Root cause likely: containers were started with `docker run` (manually) instead of docker-compose, Traefik may not pick up labels from manually started containers
- **Fix approach**:
  1. Upgrade docker-compose on server (v1.29.2 has ContainerConfig bug with newer Docker)
  2. OR install docker compose v2 plugin
  3. Then use `docker-compose -f docker-compose.prod.yml up -d` properly
- Also need to ensure API container is in BOTH `prayback_default` and `traefik` networks

## Additional Fix Applied
- Added `"type": "module"` to apps/api/package.json
- Multiple Dockerfile fixes for pnpm hoisted deps (use `pnpm install --frozen-lockfile` full, `npx tsc`)

## Server State
- Containers running: prayback_api_1, prayback_db_1 (started manually via docker run)
- Repo cloned at: /opt/stacks/prayback/
- .env has production POSTGRES_PASSWORD

## After Fixing
1. Verify `curl https://kiek-mal.de/health` works
2. Mark plan 01-03 complete, create SUMMARY
3. Run phase verification
4. Proceed to Phase 2: Event Discovery
