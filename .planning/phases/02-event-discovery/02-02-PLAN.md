---
phase: 02-event-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/tsconfig.json
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/hooks/useClustering.ts
  - apps/mobile/components/ClusterMarker.tsx
  - apps/mobile/lib/queryClient.ts
autonomous: true
requirements: [DISC-01]

must_haves:
  truths:
    - "Expo SDK 52 App startet ohne Fehler"
    - "MapView wird mit Apple Maps (iOS) angezeigt"
    - "Marker werden auf der Karte geclustert dargestellt"
  artifacts:
    - path: "apps/mobile/app/_layout.tsx"
      provides: "Root Layout mit QueryClientProvider"
      contains: "QueryClientProvider"
    - path: "apps/mobile/app/(tabs)/index.tsx"
      provides: "Karten-Tab mit MapView und Cluster-Markern"
      contains: "MapView"
    - path: "apps/mobile/hooks/useClustering.ts"
      provides: "Supercluster-Hook fuer Map Clustering"
      contains: "Supercluster"
    - path: "apps/mobile/lib/queryClient.ts"
      provides: "QueryClient Konfiguration"
      contains: "QueryClient"
  key_links:
    - from: "apps/mobile/app/(tabs)/index.tsx"
      to: "apps/mobile/hooks/useClustering.ts"
      via: "useClustering Hook"
      pattern: "useClustering"
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/lib/queryClient.ts"
      via: "QueryClient import"
      pattern: "queryClient"
---

<objective>
Expo SDK 52 Mobile-App initialisieren mit MapView und Supercluster-basiertem Marker-Clustering

Purpose: Die Mobile-App ist aktuell ein leeres Geruest — hier wird das echte Expo-Projekt aufgesetzt und die Kartenansicht als Kern-Feature erstellt
Output: Lauffaehige Expo-App mit Karte, geclusterten Markern und TanStack Query Setup
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-event-discovery/02-RESEARCH.md

<interfaces>
<!-- Bestehende Mobile-App ist ein leeres Geruest -->
<!-- apps/mobile hat nur: package.json (202 bytes), app.json (57 bytes), node_modules -->
<!-- Kein Expo SDK, kein React Native, kein expo-router installiert -->

From packages/types/src/index.ts (nach Plan 02-01):
```typescript
export interface EventListItemDTO {
  id: string;
  title: string;
  startsAt: string;
  endsAt?: string;
  category: EventCategory;
  location: { lat: number; lon: number; address: string };
  congregationId: string;
  imageUrl?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expo SDK 52 initialisieren mit expo-router und react-native-maps</name>
  <files>apps/mobile/package.json, apps/mobile/app.json, apps/mobile/tsconfig.json</files>
  <action>
    HINWEIS: NICHT `npx create-expo-app` verwenden — das wuerde die Monorepo-Struktur ueberschreiben. Stattdessen manuell installieren.

    1. In `apps/mobile/` die Expo-Dependencies installieren:
       ```bash
       cd apps/mobile
       npx expo install expo@~52 expo-router react-native react-native-web \
         react-native-safe-area-context react-native-screens \
         react-native-gesture-handler expo-linking expo-constants \
         expo-status-bar
       npx expo install react-native-maps
       pnpm add @mapbox/supercluster
       pnpm add -D @types/mapbox__supercluster typescript @types/react
       pnpm add @tanstack/react-query
       ```

    2. `apps/mobile/app.json` vollstaendig konfigurieren:
       ```json
       {
         "expo": {
           "name": "Kiek mal",
           "slug": "kiek-mal",
           "version": "0.1.0",
           "scheme": "kiekmal",
           "orientation": "portrait",
           "newArchEnabled": true,
           "ios": {
             "supportsTablet": false,
             "bundleIdentifier": "de.kiek-mal.app",
             "infoPlist": {
               "LSApplicationQueriesSchemes": ["maps", "comgooglemaps"],
               "NSLocationWhenInUseUsageDescription": "Kiek mal zeigt dir kirchliche Events in deiner Naehe"
             }
           },
           "android": {
             "adaptiveIcon": { "backgroundColor": "#ffffff" },
             "package": "de.kiekmal.app"
           },
           "plugins": ["expo-router"]
         }
       }
       ```

    3. `apps/mobile/tsconfig.json` erstellen:
       ```json
       {
         "extends": "expo/tsconfig.base",
         "compilerOptions": {
           "strict": true,
           "paths": {
             "@prayback/types": ["../../packages/types/src"]
           }
         }
       }
       ```

    4. `apps/mobile/package.json` pruefen: `"main"` muss auf `"expo-router/entry"` zeigen. Name muss `"@prayback/mobile"` sein.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/prayback/apps/mobile && npx expo config --type public 2>&1 | head -5</automated>
  </verify>
  <done>Expo SDK 52 installiert, app.json konfiguriert, New Architecture aktiv</done>
</task>

<task type="auto">
  <name>Task 2: MapView mit Supercluster-Clustering und QueryClient Setup</name>
  <files>apps/mobile/app/_layout.tsx, apps/mobile/app/(tabs)/_layout.tsx, apps/mobile/app/(tabs)/index.tsx, apps/mobile/hooks/useClustering.ts, apps/mobile/components/ClusterMarker.tsx, apps/mobile/lib/queryClient.ts</files>
  <action>
    1. Erstelle `apps/mobile/lib/queryClient.ts`:
       - QueryClient mit staleTime 5min, gcTime 24h
       - Nur QueryClient, MMKV-Persister kommt in Plan 02-04

    2. Erstelle `apps/mobile/app/_layout.tsx`:
       - Root Layout mit `QueryClientProvider` wrapping `<Stack />`
       - SafeAreaProvider

    3. Erstelle `apps/mobile/app/(tabs)/_layout.tsx`:
       - Tab-Navigation mit einem Tab "Karte" (icon: map)
       - Weitere Tabs koennen spaeter hinzugefuegt werden

    4. Erstelle `apps/mobile/hooks/useClustering.ts`:
       - Exakt nach Research Pattern 3 (supercluster Hook)
       - Input: Points[], Region, mapDimensions
       - Output: { clusters, supercluster }
       - Supercluster config: radius 60, maxZoom 16

    5. Erstelle `apps/mobile/components/ClusterMarker.tsx`:
       - Zeigt Cluster-Anzahl in einem runden Badge
       - Props: coordinate, pointCount, onPress
       - Styling: blauer Kreis mit weisser Zahl

    6. Erstelle `apps/mobile/app/(tabs)/index.tsx`:
       - MapView mit `provider={PROVIDER_DEFAULT}` (Apple Maps auf iOS — WICHTIG, nicht Google Maps wegen Expo Go Bug)
       - `tracksViewChanges={false}` auf allen Markern (Android Marker-Bug Workaround)
       - `onRegionChangeComplete` updated Region-State
       - useClustering Hook mit Mock-Daten (echte API-Anbindung kommt in Plan 02-03)
       - Cluster-Marker und einzelne Event-Marker rendern
       - Initiale Region: Nordfriesland/Dithmarschen (lat: 54.12, lon: 8.85)
       - Mock-Daten: 5-10 hardcoded Events mit verschiedenen Koordinaten in Dithmarschen
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/prayback && pnpm turbo typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>MapView zeigt Karte mit geclusterten Mock-Markern, App startet ohne Fehler</done>
</task>

</tasks>

<verification>
```bash
# Typecheck
pnpm turbo typecheck

# Expo Config valid
cd apps/mobile && npx expo config --type public

# App starten (manuell in Expo Go)
cd apps/mobile && npx expo start
```
</verification>

<success_criteria>
- Expo SDK 52 App startet in Expo Go ohne Crash
- MapView zeigt Karte zentriert auf Dithmarschen
- Mock-Marker werden geclustert dargestellt
- Zoom in/out aendert Cluster-Zusammensetzung
- TypeScript kompiliert ohne Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/02-event-discovery/02-02-SUMMARY.md`
</output>
