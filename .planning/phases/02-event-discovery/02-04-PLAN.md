---
phase: 02-event-discovery
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - apps/mobile/lib/queryClient.ts
  - apps/mobile/app/_layout.tsx
  - apps/mobile/package.json
autonomous: true
requirements: [DISC-06]

must_haves:
  truths:
    - "Zuletzt geladene Events sind ohne Internetverbindung sichtbar"
    - "Karte zeigt gecachte Daten im Offline-Modus"
    - "Nach App-Neustart sind gecachte Events sofort verfuegbar"
  artifacts:
    - path: "apps/mobile/lib/queryClient.ts"
      provides: "QueryClient mit MMKV Persister"
      contains: "createSyncStoragePersister"
    - path: "apps/mobile/app/_layout.tsx"
      provides: "PersistQueryClientProvider statt QueryClientProvider"
      contains: "PersistQueryClientProvider"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/lib/queryClient.ts"
      via: "PersistQueryClientProvider mit persister"
      pattern: "PersistQueryClientProvider"
    - from: "apps/mobile/lib/queryClient.ts"
      to: "react-native-mmkv"
      via: "MMKV Storage Adapter"
      pattern: "MMKV"
---

<objective>
Offline-Cache mit MMKV und TanStack Query Persistence fuer Events und Kartendaten

Purpose: Nutzer koennen zuletzt geladene Events und die Karte auch ohne Internet sehen (DISC-06)
Output: MMKV-basierter persistenter Query-Cache der App-Neustarts und Offline-Phasen ueberlebt
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-event-discovery/02-RESEARCH.md
@.planning/phases/02-event-discovery/02-03-SUMMARY.md

<interfaces>
<!-- Von Plan 02-02/02-03: Bestehende Mobile-Struktur -->

From apps/mobile/lib/queryClient.ts (erstellt in 02-02):
```typescript
import { QueryClient } from "@tanstack/react-query";

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000,
      gcTime: 24 * 60 * 60 * 1000,
    },
  },
});
```

From apps/mobile/app/_layout.tsx (erstellt in 02-02):
```typescript
// Verwendet aktuell QueryClientProvider
// Muss auf PersistQueryClientProvider umgestellt werden
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MMKV installieren und QueryClient mit Persister erweitern</name>
  <files>apps/mobile/package.json, apps/mobile/lib/queryClient.ts</files>
  <action>
    1. Dependencies installieren:
       ```bash
       cd apps/mobile
       pnpm add react-native-mmkv @tanstack/query-sync-storage-persister @tanstack/react-query-persist-client
       ```

    2. `apps/mobile/lib/queryClient.ts` erweitern:
       - Exakt nach Research Pattern 4
       - MMKV-Instanz mit id "query-cache" erstellen
       - mmkvStorageAdapter mit setItem/getItem/removeItem
       - createSyncStoragePersister mit storage: mmkvStorageAdapter, throttleTime: 3000
       - `persister` exportieren (neben bestehendem queryClient)
       - gcTime auf 24h belassen (wichtig fuer Offline-Nutzung)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/prayback && pnpm turbo typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>MMKV Persister konfiguriert und exportiert</done>
</task>

<task type="auto">
  <name>Task 2: Root Layout auf PersistQueryClientProvider umstellen</name>
  <files>apps/mobile/app/_layout.tsx</files>
  <action>
    1. In `apps/mobile/app/_layout.tsx`:
       - `QueryClientProvider` durch `PersistQueryClientProvider` ersetzen (Import aus @tanstack/react-query-persist-client)
       - `persister` aus lib/queryClient importieren
       - `persistOptions={{ persister }}` als Prop setzen
       - Restliche Layout-Struktur beibehalten

    Ergebnis:
    ```tsx
    <PersistQueryClientProvider client={queryClient} persistOptions={{ persister }}>
      {/* bestehender Layout-Baum */}
    </PersistQueryClientProvider>
    ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/prayback && pnpm turbo typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>App nutzt PersistQueryClientProvider, gecachte Queries ueberleben App-Neustart</done>
</task>

</tasks>

<verification>
```bash
# Typecheck
pnpm turbo typecheck

# Manueller Offline-Test:
# 1. App starten, Events laden lassen
# 2. Flugmodus aktivieren
# 3. App schliessen und neu oeffnen
# 4. Gecachte Events muessen noch sichtbar sein
```
</verification>

<success_criteria>
- MMKV Storage Persister ist aktiv (throttleTime 3000ms)
- PersistQueryClientProvider wrapped die gesamte App
- Events sind nach App-Neustart sofort aus Cache verfuegbar
- Im Flugmodus zeigt die App zuletzt geladene Events
</success_criteria>

<output>
After completion, create `.planning/phases/02-event-discovery/02-04-SUMMARY.md`
</output>
