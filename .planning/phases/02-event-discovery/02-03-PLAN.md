---
phase: 02-event-discovery
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - apps/mobile/hooks/useEvents.ts
  - apps/mobile/hooks/useCongregation.ts
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/events/[id].tsx
  - apps/mobile/app/congregations/[id].tsx
  - apps/mobile/components/FilterBar.tsx
  - apps/mobile/lib/api.ts
  - apps/mobile/lib/mapsNavigation.ts
autonomous: false
requirements: [DISC-02, DISC-03, DISC-04, DISC-05, DISC-07]

must_haves:
  truths:
    - "Karte zeigt Events aus der echten API statt Mock-Daten"
    - "User kann Events nach Kategorie filtern und Karte aktualisiert sich"
    - "User kann Events nach Radius filtern"
    - "User kann auf einen Marker tippen und sieht Event-Details (alle Felder)"
    - "User kann Navigation starten und wird an Google/Apple Maps weitergeleitet"
    - "User kann Gemeindeprofil mit kommenden Events sehen"
  artifacts:
    - path: "apps/mobile/hooks/useEvents.ts"
      provides: "TanStack Query Hook fuer Events-API"
      contains: "useQuery"
    - path: "apps/mobile/app/events/[id].tsx"
      provides: "Event-Detailseite mit allen Feldern"
      contains: "price"
    - path: "apps/mobile/app/congregations/[id].tsx"
      provides: "Gemeindeprofil-Screen"
      contains: "events"
    - path: "apps/mobile/components/FilterBar.tsx"
      provides: "Kategorie- und Radius-Filter UI"
      contains: "category"
    - path: "apps/mobile/lib/mapsNavigation.ts"
      provides: "Maps Navigation Deep Link Helper"
      contains: "Linking.openURL"
  key_links:
    - from: "apps/mobile/hooks/useEvents.ts"
      to: "https://kiek-mal.de/events"
      via: "fetch mit Query-Params"
      pattern: "kiek-mal.de/events"
    - from: "apps/mobile/app/events/[id].tsx"
      to: "apps/mobile/lib/mapsNavigation.ts"
      via: "Navigation starten Button"
      pattern: "openMapsNavigation"
    - from: "apps/mobile/app/(tabs)/index.tsx"
      to: "apps/mobile/hooks/useEvents.ts"
      via: "useEvents Hook statt Mock-Daten"
      pattern: "useEvents"
---

<objective>
API-Anbindung, Event-Details, Filter, Gemeindeprofil und Maps-Navigation in der Mobile-App

Purpose: Die Karte wird von Mock-Daten auf echte API umgestellt, Event-Details und Gemeindeprofil werden als Screens hinzugefuegt, und der User kann filtern und navigieren
Output: Vollstaendige Event-Discovery-Experience mit allen DISC-Requirements (ausser Offline)
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-event-discovery/02-RESEARCH.md
@.planning/phases/02-event-discovery/02-01-SUMMARY.md
@.planning/phases/02-event-discovery/02-02-SUMMARY.md

<interfaces>
<!-- Von Plan 02-01: API Endpoints -->
<!-- GET /events?lat=X&lon=Y&radius=Z&category=C&dateFrom=D -->
<!-- GET /congregations/:id -->

<!-- Von Plan 02-02: Bestehende Mobile-Struktur -->
<!-- apps/mobile/app/(tabs)/index.tsx — MapView mit useClustering und Mock-Daten -->
<!-- apps/mobile/hooks/useClustering.ts — Supercluster Hook -->
<!-- apps/mobile/lib/queryClient.ts — QueryClient -->

From packages/types/src/index.ts (erweitert in 02-01):
```typescript
export interface EventDTO {
  id: string;
  title: string;
  startsAt: string;
  endsAt?: string;
  category: EventCategory;
  location: { lat: number; lon: number; address: string };
  congregationId: string;
  description?: string;
  imageUrl?: string;
  source: "manual" | "churchdesk";
  price?: string;
  registrationUrl?: string;
  bringItems?: string;
  persons?: string;
}

export interface EventListItemDTO { /* schlanke Version fuer Liste */ }
export interface CongregationDetailDTO extends CongregationDTO { events: EventDTO[] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API-Client, useEvents Hook und FilterBar</name>
  <files>apps/mobile/lib/api.ts, apps/mobile/hooks/useEvents.ts, apps/mobile/components/FilterBar.tsx, apps/mobile/app/(tabs)/index.tsx</files>
  <action>
    1. Erstelle `apps/mobile/lib/api.ts`:
       - Base URL: `https://kiek-mal.de` (Production API)
       - `fetchEvents(params: { lat, lon, radius?, category?, dateFrom?, dateTo? })` — GET /events mit Query-Params
       - `fetchCongregation(id: string)` — GET /congregations/:id
       - Typen aus `@prayback/types` importieren

    2. Erstelle `apps/mobile/hooks/useEvents.ts`:
       - `useEvents({ lat, lon, radius, category, dateFrom })` — TanStack useQuery mit fetchEvents
       - Query-Key: `['events', { lat, lon, radius, category, dateFrom }]`
       - enabled: nur wenn lat/lon gesetzt

    3. Erstelle `apps/mobile/components/FilterBar.tsx`:
       - Horizontal scrollbare Leiste am oberen Kartenrand (ueber der Karte, halbtransparent)
       - Kategorie-Chips: "Alle", "Gottesdienst", "Konzert", "Jugend", "Gemeindeleben", "Lesung", "Diskussion", "Andacht"
       - Aktiver Chip visuell hervorgehoben
       - Radius-Auswahl: Dropdown oder Chips (5km, 10km, 25km, 50km)
       - Props: `onCategoryChange(category?: EventCategory)`, `onRadiusChange(radius: number)`
       - Styling: StyleSheet (sicher verfuegbar, kein NativeWind noetig)

    4. In `apps/mobile/app/(tabs)/index.tsx`:
       - Mock-Daten durch useEvents Hook ersetzen
       - FilterBar ueber der MapView rendern (absolute Position)
       - Kategorie- und Radius-State verwalten und an useEvents weiterreichen
       - Region-Center als lat/lon fuer API verwenden
       - Beim Tippen auf einen einzelnen Marker: Navigation zu `/events/[id]`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/prayback && pnpm turbo typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>Karte zeigt echte Events aus API, Filter aendern die angezeigten Events</done>
</task>

<task type="auto">
  <name>Task 2: Event-Detailseite, Gemeindeprofil und Maps-Navigation</name>
  <files>apps/mobile/app/events/[id].tsx, apps/mobile/app/congregations/[id].tsx, apps/mobile/hooks/useCongregation.ts, apps/mobile/lib/mapsNavigation.ts</files>
  <action>
    1. Erstelle `apps/mobile/lib/mapsNavigation.ts`:
       - Exakt nach Research Pattern 5
       - `openMapsNavigation(lat, lon, label)` — Platform.select fuer iOS (maps://) und Android (geo:)
       - Fallback auf Google Maps Web-URL

    2. Erstelle `apps/mobile/hooks/useCongregation.ts`:
       - `useCongregation(id: string)` — TanStack useQuery mit fetchCongregation
       - Query-Key: `['congregations', id]`

    3. Erstelle `apps/mobile/app/events/[id].tsx`:
       - Expo Router dynamic route — ID aus `useLocalSearchParams()`
       - Event-Daten aus dem QueryClient-Cache laden (die Events sind schon vom useEvents geladen)
         ODER einzeln von der API fetchen (GET /events Response enthaelt alle Felder)
       - Alle Felder anzeigen: Titel (gross), Datum+Uhrzeit, Ort/Adresse, Kategorie-Badge, Beschreibung, Bild (wenn vorhanden), Preis, Personen, Mitbringen-Liste, Anmelde-Link
       - "Navigation starten" Button — ruft openMapsNavigation auf
       - "Gemeinde anzeigen" Link — navigiert zu /congregations/[congregationId]
       - ScrollView Layout, saubere Typografie

    4. Erstelle `apps/mobile/app/congregations/[id].tsx`:
       - Expo Router dynamic route
       - useCongregation Hook fuer Daten
       - Anzeige: Name, Adresse, Website-Link (oeffnet in Browser), kommende Events als Liste
       - Tippen auf Event in der Liste navigiert zu /events/[id]
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/prayback && pnpm turbo typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>Event-Detailseite zeigt alle Felder, Navigation leitet an Maps weiter, Gemeindeprofil mit Events sichtbar</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Event Discovery Experience verifizieren</name>
  <files>none</files>
  <action>
    User verifiziert die komplette Event-Discovery-Experience in Expo Go:
    Karte mit echten Events, Filter, Detailseite, Gemeindeprofil, Navigation
  </action>
  <verify>
    1. App in Expo Go starten: `cd apps/mobile && npx expo start`
    2. Karte sollte auf Dithmarschen zentriert sein
    3. Falls Events vorhanden: Marker/Cluster auf der Karte sichtbar
    4. Filter-Chips antippen — Events filtern sich
    5. Auf einen Marker tippen — Event-Detailseite oeffnet sich
    6. Alle Felder pruefen: Titel, Datum, Beschreibung, etc.
    7. "Navigation starten" antippen — Maps-App oeffnet sich
    8. Zurueck, "Gemeinde anzeigen" antippen — Gemeindeprofil sichtbar
  </verify>
  <done>User hat "approved" geschrieben oder Issues beschrieben</done>
</task>

</tasks>

<verification>
```bash
# Typecheck
pnpm turbo typecheck

# App manuell testen
cd apps/mobile && npx expo start
```
</verification>

<success_criteria>
- Karte zeigt echte Events aus der API (keine Mock-Daten)
- Kategorie-Filter aendert angezeigte Events
- Radius-Filter aendert Suchbereich
- Event-Detailseite zeigt alle Felder inkl. price, persons, bringItems
- "Navigation starten" oeffnet Google/Apple Maps
- Gemeindeprofil zeigt Name, Adresse, Website, kommende Events
</success_criteria>

<output>
After completion, create `.planning/phases/02-event-discovery/02-03-SUMMARY.md`
</output>
