{
  "name": "@prayback/mobile",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'mobile build skip in CI'"
  },
  "dependencies": {
    "@prayback/types": "workspace:*"
  }
}
